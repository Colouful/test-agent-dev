# Lab 10-13 操作手册

> 使用方式：在另一个终端开启 Claude Code，把"执行提示词"复制过去执行；完成后用"验证提示词"确认结果。

---

## 总体方案概览

| Lab | 核心产出 | 耗时 | 前置依赖 |
|-----|---------|------|----------|
| **10** | PostToolUse hook + test-runner 子代理 + exit-contract | ~15min | 无 |
| **11** | 记忆 Pipeline（capture/retrieve/distill/evolve） | ~20min | Lab 10 产出 |
| **12** | ci/verify.sh（三层断言验证体系） | ~15min | recognition_service.py |
| **13** | loop/worker-loop.sh（熔断+记忆+2-strike） | ~15min | Lab 12 的 verify.sh |

操作顺序：

```
Lab 10 ──→ Lab 11 ──→ Lab 12 ──→ Lab 13
(hook)    (memory)   (verify)   (loop)
                         ↑           ↑
                         └───────────┘
                    Lab 13 依赖 verify.sh
```

---

## 准备工作：清空现有产出

在开始之前，先执行以下命令清空旧产出（保留 backend 和 spec 不动）：

```bash
cd /workshop/ypjh

# 备份旧文件（以防万一）
mkdir -p /tmp/lab-backup
cp -r hooks/ loop/ subagents/ .claude/hooks/ /tmp/lab-backup/ 2>/dev/null

# 清空 Lab 10-13 产出
rm -rf hooks/
rm -rf .claude/hooks/
rm -rf loop/
rm -rf subagents/
rm -rf .claude/agents/
rm -rf .claude/settings.json
rm -f memory/exit-contract.md memory/progress.md
rm -rf memory/daily/
rm -f memory/corrections.md memory/MEMORY.md memory/archive.md
rm -rf skills/distill-memory/ skills/evolve-memory/ skills/memory-health/
rm -f rules/proposed.md

# 保留的（不动）：
# - backend/ (recognition_service.py + tests)
# - ci/verify.sh, ci/arch-check.sh
# - spec/
# - rules/personal.md, rules/severity-guide.md, rules/architecture.md
# - .claude/settings.local.json (AWS 权限配置)
```

---

# Lab 10 — 编排流水线（Hook + 子代理 + Exit-Contract）

## 总体方案

三层递进：
1. **Layer 1** — 配 PostToolUse hook，编辑 .py 文件后自动跑架构检查
2. **Layer 2** — 定义 test-runner 子代理，隔离测试上下文
3. **Layer 3** — Exit-contract 交接模板 + 人类 Gate 规则

---

## 执行提示词 #1（Layer 1: Hook）

```
我在 /workshop/ypjh 项目下工作。帮我配置 Claude Code 的 PostToolUse hook：

1. 创建 hooks/post-edit-check.sh 脚本：
   - 当编辑的文件是 .py 后缀时，自动运行 ci/arch-check.sh
   - 如果 arch-check.sh 返回非零（ARCH_FAIL），输出错误信息并 exit 1
   - 非 .py 文件静默通过（exit 0）
   - set -uo pipefail
   - 从第一个参数 $1 获取文件路径

2. 创建 .claude/settings.json，配置 hooks：
   {
     "hooks": {
       "PostToolUse": [{
         "matcher": "Edit",
         "hooks": [{
           "type": "command",
           "command": "bash /workshop/ypjh/hooks/post-edit-check.sh"
         }]
       }]
     }
   }

3. 确保 hooks/ 目录存在，脚本有执行权限（chmod +x）
4. 不要修改 ci/arch-check.sh 本身
```

---

## 执行提示词 #2（Layer 2: 子代理）

```
在 /workshop/ypjh 项目下，帮我创建 test-runner 子代理：

1. 创建 .claude/agents/test-runner.md（确保 .claude/agents/ 目录存在）：

---
name: test-runner
description: 运行测试套件并结构化报告结果。当需要执行测试、定位失败原因时委派给它。
tools: Bash, Read, Grep
---

# 角色：测试执行者

## 职责
1. 运行 ci/verify.sh 或 python3 -m pytest backend/tests/ -v
2. 解析结果，结构化报告：通过数 / 失败数 / 每个失败的文件:行 + 原因
3. **不修改任何实现代码** — 只负责跑和报告，修复交回主 agent

## 输出格式

TEST REPORT
===========
suite: <测试文件>
passed: N
failed: M

FAILURES:
- <test_name>: <一句话原因>

VERDICT: ALL PASS / N FAILURES — <修复建议>

## 红线
- 绝不改实现代码
- 绝不改测试代码
- 只用 Bash/Read/Grep 三个工具

2. 确保文件权限正确
```

---

## 执行提示词 #3（Layer 3: Exit-Contract + Human Gate）

```
在 /workshop/ypjh 项目下，帮我建立会话交接机制和人类 Gate：

1. 创建 memory/exit-contract.template.md：

# 会话交接契约

## 本次完成了什么
- (列出已完成的具体事项)

## 进行中 — 下次从这里接
- (正在做但没做完的事，精确到文件和函数)

## 已尝试但失败的方案（别再走这条路）
- (方案描述 + 为什么失败)

## 关键决策及理由
- (做了什么选择 + 为什么这样选)

## 下一步建议
- (具体的 1-3 个 action items)

2. 在 rules/personal.md 末尾追加"人类 Gate 规则"段：

## 人类 Gate 规则

以下操作必须暂停并等待我明确确认，绝不自动执行：
- git push（任何推送到远端）
- 删除文件（rm / unlink）
- 修改 CI 配置文件
- 调用外部 API（真实环境，非 mock）
- aws 任何写入操作

以下操作可以自动执行，不需要问我：
- git add / commit（本地提交）
- 创建/编辑文件
- 运行测试
- 读取文件

3. 创建 hooks/session-start.sh 脚本：
   - 如果 memory/exit-contract.md 存在且非空，cat 输出其内容（前缀"--- 上次交接 ---"）
   - 如果 memory/progress.md 存在且非空，cat 输出其内容（前缀"--- 进度记忆 ---"）
   - 脚本加执行权限
   - set -uo pipefail

4. 把 SessionStart hook 加入 .claude/settings.json（和已有的 PostToolUse 合并，不要覆盖）
```

---

## 验证提示词（Lab 10 全部做完后）

```
帮我验证 Lab 10 的产出是否完整：

1. 检查 hooks/post-edit-check.sh 是否存在且可执行：
   - ls -la hooks/post-edit-check.sh
   - 运行：bash hooks/post-edit-check.sh（无参数应该 exit 0）

2. 检查子代理：
   - cat .claude/agents/test-runner.md（应该有 name/description/tools 头部）

3. 检查 settings.json：
   - cat .claude/settings.json（应该有 PostToolUse + SessionStart 两个 hook）

4. 检查交接模板：
   - cat memory/exit-contract.template.md（5 个 section）

5. 检查 rules/personal.md 末尾是否有"人类 Gate 规则"段

6. 运行架构检查确认 hook 能正常工作：
   - bash ci/arch-check.sh（预期 ARCH_OK 或 SKIP）

报告每一项的状态：通过 / 缺失
```

---

# Lab 11 — 记忆 Pipeline（Capture + Distill + Evolve）

## 总体方案

三层递进：
1. **Layer 1** — Capture（Stop hook 记 git 痕迹 + UserPromptSubmit hook 捕获纠正）+ Retrieve（SessionStart 注入记忆）
2. **Layer 2** — Distill skill（日志→教训）+ Evolve skill（纠正→规则提案）
3. **Layer 3** — 智能注入（按需提醒蒸馏）+ memory-health 健康报告

---

## 执行提示词 #1（Layer 1: Capture + Retrieve）

```
在 /workshop/ypjh 项目下，帮我配置完整的记忆捕获和注入机制：

1. 创建目录 memory/daily/（如果不存在）
2. 创建空文件 memory/corrections.md（如果不存在）

3. 创建 .claude/hooks/capture-session.sh（Stop hook，会话结束时触发）：
   - 记录日期时间
   - 用 git diff --name-only HEAD~1 2>/dev/null 记录本次改了哪些文件
   - 用 git log --oneline -3 --no-decorate 2>/dev/null 记录最近的 commit
   - 把结果追加到 memory/daily/$(date +%Y-%m-%d).md，格式：
     ## HH:MM | session
     **改动文件：** file1, file2
     **Git：** commit messages
   - 脚本要 set -uo pipefail，出错不阻塞（命令末尾加 || true）
   - 加执行权限

4. 创建 .claude/hooks/capture-correction.sh（UserPromptSubmit hook）：
   - 从 stdin 读取 JSON（Claude Code 会传入含 prompt 的 JSON）
   - 用 jq 提取 .input.content（用户消息文本）
   - 如果匹配纠正信号词（不对|应该是|错了|别这么|搞反|不是这样|wrong|should be|don't|stop|revert），
     则追加到 memory/corrections.md，格式：
     - [YYYY-MM-DD HH:MM] 用户原文（前120字）
   - 不匹配则静默退出（exit 0，不输出）
   - 如果没有 jq 可用，用 grep/sed 做简单提取作为降级
   - 加执行权限

5. 创建 .claude/hooks/inject-memory.sh（SessionStart hook）：
   - 如果 memory/MEMORY.md 存在，cat 输出它的内容
   - 如果 rules/personal.md 存在，cat 输出它的内容
   - 如果 memory/corrections.md 存在且非空，输出"## 近期纠正（待进化）"后跟最近 10 条
   - 如果 memory/exit-contract.md 存在，输出 "--- exit-contract ---" + 内容
   - 如果 memory/progress.md 存在，输出 "--- progress ---" + 内容
   - 脚本 stdout 的内容会被 Claude Code 注入为会话上下文
   - 加执行权限

6. 更新 .claude/settings.json（与 Lab 10 已有配置合并，不要覆盖 PostToolUse）：
   {
     "hooks": {
       "PostToolUse": [已有的 Edit hook 保留],
       "Stop": [{
         "matcher": "",
         "hooks": [{
           "type": "command",
           "command": "bash /workshop/ypjh/.claude/hooks/capture-session.sh"
         }]
       }],
       "UserPromptSubmit": [{
         "matcher": "",
         "hooks": [{
           "type": "command",
           "command": "bash /workshop/ypjh/.claude/hooks/capture-correction.sh"
         }]
       }],
       "SessionStart": [{
         "matcher": "",
         "hooks": [{
           "type": "command",
           "command": "bash /workshop/ypjh/.claude/hooks/inject-memory.sh"
         }]
       }]
     }
   }

确保 .claude/hooks/ 目录存在，所有脚本有执行权限。
```

---

## 执行提示词 #2（Layer 2: Distill + Evolve Skills）

```
在 /workshop/ypjh 项目下，帮我创建两个 skill：

## Skill 1: distill-memory

创建 skills/distill-memory/SKILL.md：

---
name: distill-memory
description: 将 memory/daily/ 的原始日志蒸馏为 memory/MEMORY.md 的持久条目
tools: Read, Write, Glob, Grep
---

# 记忆蒸馏

## 设计参照
参照 SwarmAI distillation_hook 的两阶段架构：
- 阶段1：从 daily 日志提取结构化信号（decisions, lessons, patterns）
- 阶段2：频率门控 + 去重后写入 MEMORY.md

## 触发条件
memory/daily/ 下有 >=3 个未标记 <!--distilled--> 的日志文件

## 提取三类条目

### 1. 关键决策（Key Decisions）
识别模式：decided to / chose / will use / going with / switched to
格式：- [YYYY-MM-DD] **决策标题** — 为什么这样选择（保留 why）

### 2. 教训（Lessons Learned）
识别模式：lesson learned / fixed by / root cause was / should have / next time
格式：- [YYYY-MM-DD] **教训标题** — 具体怎么避免

### 3. 复发模式（Recurring Patterns）
条件：同类事件在不同日志中出现 >=2 次（频率门控）
格式：- [YYYY-MM-DD] **模式名** — 出现N次，建议动作

## 写入规则
- 追加（prepend）到 memory/MEMORY.md 对应 section
- 去重：如果 MEMORY.md 已有相同标题的条目，跳过不写
- 每条必须对未来会话可直接用（能指导判断，不是流水账）
- 蒸馏完给 daily 文件尾部加 <!--distilled YYYY-MM-DD-->

## 铁律
- 只提炼"学到了什么"，不照抄"发生了什么"
- 不删 daily（留待 TTL 过期或手动归档）
- 总 MEMORY.md 体积控制在 <30K token（超出时标记最旧条目为 dormant）

---

## Skill 2: evolve-memory

创建 skills/evolve-memory/SKILL.md：

---
name: evolve-memory
description: 扫描 corrections，把复发纠正提案为规则；标记过期记忆为 dormant
tools: Read, Write, Grep
---

# 记忆进化（硬化 + 遗忘）

## 设计参照
参照 SwarmAI 的三层机制：
- distillation_hook._extract_corrections() → 提取纠正
- steeringify.group_and_propose() → 聚类+提案
- evolution_maintenance_hook → 衰减过期条目

## 硬化：纠正 → 规则提案

1. 读 memory/corrections.md，按相似主题归类
2. 同类纠正出现 >=3 次 → 生成规则提案到 rules/proposed.md：
   - **规则文本**：一句话、可判定、格式参照 rules/personal.md 现有条目
   - **级别**：MUST（会造成 bug/安全问题）/ SHOULD（偏好/风格）
   - **证据**：引用触发的纠正记录（日期+原文摘要）
   - **建议硬化层级**：
     - L1 文字规则（首次）
     - L2 hook 门控（复发 5+ 次）
     - L3 结构性不可能（反复跨项目出现）
3. **绝不**直接写入 rules/personal.md 或 CLAUDE.md

## 遗忘：达尔文式衰减

4. 扫 memory/MEMORY.md：
   - 条目日期 >60 天 且 该条目的关键词在最近 5 个 daily 日志中没出现过
     → 在条目末尾追加 <!--dormant YYYY-MM-DD-->
   - 已 dormant 超过 30 天 → 移到 memory/archive.md（不删）

## 铁律
- 执行者不是立法者：只写 proposed.md，人审后手动转正
- 遗忘 ≠ 删除：dormant 降权不删，archive 留可查
- 被推翻的旧决策：标 superseded_by + 降权

确保两个 skill 目录都存在。
```

---

## 执行提示词 #3（Layer 3: 智能注入升级 + 健康报告）

```
在 /workshop/ypjh 项目下，做两件事：

## 1. 升级 inject-memory.sh，增加"蒸馏提醒"逻辑

修改 .claude/hooks/inject-memory.sh，在现有注入逻辑之后追加：

1. 数 memory/daily/ 下没有 <!--distilled 标记的文件数量
2. 如果 >=3 个：
   - 在注入内容末尾输出：
     "你有 N 个未蒸馏的日志。本次会话结束前，建议运行 /distill-memory 整理记忆。"
3. 如果 corrections.md 超过 10 条未处理的纠正：
   - 追加提醒："有 N 条积压纠正。建议运行 /evolve-memory 检查是否有复发模式可提案为规则。"
4. 无论如何都全量注入 MEMORY.md + personal.md + 最近 5 条 corrections

关键：提醒是建议不是强制——agent 收到提醒会在合适时机提示用户，不打断当前工作。

## 2. 创建 skills/memory-health/SKILL.md

---
name: memory-health
description: 对记忆系统做健康度报告：覆盖率、新鲜度、进化率、体积、闭环完整性
tools: Read, Glob, Grep, Bash
---

# 记忆系统健康报告

## 设计参照
参照 SwarmAI DDD Layer 2 的五维健康度评分思想，简化为记忆 pipeline 的五维。

## 五维评估

### 1. 覆盖率 Coverage
- memory/ 下有多少记忆条目？
- daily/ 积累了多少天的日志？
- corrections 有多少条？
- 评分：>20 条记忆 + >7 天日志 + >5 条纠正 = 满分

### 2. 新鲜度 Freshness
- 最近一次 daily 日志是什么时候？
- 最近一次蒸馏（有 <!--distilled--> 标记）是什么时候？
- 有多少 daily 积压未蒸馏？
- 评分：<3 天无新日志 + <3 个积压 = 满分

### 3. 进化率 Evolution Rate
- corrections 中有多少条对应了 MEMORY 条目或 rules？（被消化的比例）
- proposed.md 有多少待审提案？有多少已批准转正的？
- 评分：消化率 >50% + 有已转正规则 = 满分

### 4. 体积 Size Budget
- MEMORY.md 当前估算 token 数（word_count * 1.3）
- rules/ 所有规则总 token 数
- 总注入体积 vs 50K 上限的余量百分比
- 评分：<30K 绿 / 30-50K 黄 / >50K 红（需要遗忘或检索）

### 5. 闭环完整性 Loop Integrity
- hooks 配置是否完整（Stop + UserPromptSubmit + SessionStart）？
- skills 是否存在（distill-memory + evolve-memory）？
- MEMORY.md 是否存在且非空？
- 评分：全有 = 满分；缺任何一环 = 闭环断裂

## 输出格式

| 维度 | 得分 | 状态 | 说明 |
|------|------|------|------|
| 覆盖率 | N/100 | 绿/黄/红 | ... |
| ... | ... | ... | ... |

**总评**：飞轮健康 / 需关注（某环节积压）/ 闭环断裂

**建议下一步**：基于最低分维度给出 1-2 条具体行动
```

---

## 验证提示词（Lab 11 全部做完后）

```
帮我验证 Lab 11 的记忆 Pipeline 是否完整：

1. Hooks 检查（注意位置在 .claude/hooks/ 下）：
   - ls -la .claude/hooks/capture-session.sh（存在 + 可执行）
   - ls -la .claude/hooks/capture-correction.sh（存在 + 可执行）
   - ls -la .claude/hooks/inject-memory.sh（存在 + 可执行）

2. Settings 配置检查：
   - cat .claude/settings.json
   - 确认包含：Stop、UserPromptSubmit、SessionStart、PostToolUse 四个 hook 事件

3. Skills 检查：
   - cat skills/distill-memory/SKILL.md（存在，有 name/description/tools 头部）
   - cat skills/evolve-memory/SKILL.md（存在，有 name/description/tools 头部）
   - cat skills/memory-health/SKILL.md（存在）

4. 目录/文件检查：
   - ls memory/daily/（目录存在）
   - ls memory/corrections.md（文件存在）

5. 闭环测试：
   - 模拟 capture-session：手动执行 bash .claude/hooks/capture-session.sh
     然后检查 memory/daily/$(date +%Y-%m-%d).md 是否有内容
   - 模拟 inject-memory：bash .claude/hooks/inject-memory.sh（应有输出，不报错）

6. inject-memory 智能提醒检查：
   - inject-memory.sh 中是否有检测未蒸馏文件数量的逻辑？
   - 是否有 corrections 积压提醒逻辑？

报告每一项：通过 / 缺失
```

---

# Lab 12 — 验证体系（三层断言 + verify.sh）

## 总体方案

三层递进：
1. **Layer 1** — 体验"同义反复"陷阱（可选，体验用）
2. **Layer 2** — 写三类断言：结构 / 契约 / 统计
3. **Layer 3** — 封装为 ci/verify.sh（loop 的终点线）

注意：项目里已有 `ci/verify.sh` 和 `backend/services/recognition_service.py`。从零重做意味着重写 verify.sh。

---

## 执行提示词 #1（重写 verify.sh）

```
在 /workshop/ypjh 项目下，帮我从零重写 ci/verify.sh — 识别功能的三层验证脚本。

前置背景：
- backend/services/recognition_service.py 已经实现了识别功能（mock 模式）
- RecognitionService 有 mock_scenario 参数（"clear"/"blurry"/"empty"）
- recognize(image_bytes, user_id) 返回 RecognitionResult（含 status, candidate, error_hint）
- 关键业务规则：
  - R2: confidence 缺失时按 0.0 处理
  - R4: confidence < 0.7 → status="pending_review"
  - R9: content/answer 为空时用占位符

请重写 ci/verify.sh，设计如下：

#!/usr/bin/env bash
# ci/verify.sh — 识别功能验证体系（三层断言）
# 退出码：0=全部通过，1=断言失败，2=环境错误
set -euo pipefail

## 三层结构：

### 第 1 层：结构断言（字段存在性 + 类型）
- 对 clear/blurry/empty 三个 scenario 调用 recognize()
- 验证 status 在合法值集合内（high_confidence/pending_review/error）
- high_confidence 必须有 candidate
- candidate.confidence 是 float 且在 [0,1]
- candidate.content 非空字符串

### 第 2 层：契约断言（R2 + R4 业务规则）
- R2 契约：empty scenario 的 confidence 应为 0.0
- R4 契约：blurry scenario（低置信度）status 必须是 pending_review
- R4 契约：pending_review 结果有 error_hint
- 正常路径：clear scenario status 是 high_confidence

### 第 3 层：统计断言（N=30 次运行分布）
- clear 应全部 high_confidence
- blurry 应全部 pending_review
- empty 应全部 pending_review

## 格式要求：
- 每层前输出标题 "── 第 N 层：xxx"
- 用内嵌 Python（heredoc）执行断言
- 快速失败：前一层挂了不跑后面
- 最终输出 "VERIFY_OK — 全部 3 层断言通过" 或 "VERIFY_FAIL: xxx"
- Python 中 sys.path.insert(0, "backend") 和 sys.path.insert(0, ".")
- 使用 MOCK_BEDROCK 环境（不调真实 API）

确保脚本可执行（chmod +x）。
```

---

## 验证提示词（Lab 12）

```
帮我验证 Lab 12 的 verify.sh 是否正确：

1. 脚本存在且可执行：
   ls -la ci/verify.sh

2. 运行验证（应该全部通过）：
   cd /workshop/ypjh && bash ci/verify.sh
   echo "exit code: $?"

3. 检查输出结构：
   - 应有"第 1 层：结构断言"
   - 应有"第 2 层：契约断言"
   - 应有"第 3 层：统计断言"
   - 最终应有"VERIFY_OK"

4. 破坏测试（验证能抓到错误）：
   - 临时把 recognition_service.py 中的 CONFIDENCE_THRESHOLD 改成 0.0
   - 再跑 ci/verify.sh，应该失败（VERIFY_FAIL）
   - 改回来

报告：三层全绿 / 哪层失败了
```

---

# Lab 13 — Worker Loop（熔断 + 记忆 + 2-Strike）

## 总体方案

三层递进：
1. **Layer 1** — 最小 loop：调 claude -p → 跑 verify → 回灌错误
2. **Layer 2** — 熔断三件套：MAX_ITER + MAX_SECONDS + 退出码语义
3. **Layer 3** — STATE.md 记忆 + 2-strike 规则

---

## 执行提示词 #1（完整 worker-loop.sh，含三层）

```
在 /workshop/ypjh 项目下，帮我创建 loop/worker-loop.sh — 完整的 Worker Loop 脚本。
确保 loop/ 目录存在。

要求实现以下完整功能（一次性写好三层）：

#!/usr/bin/env bash
# loop/worker-loop.sh — Worker Loop（熔断 + 记忆 + 2-Strike）
#
# 退出码：
#   0 = 验证通过（修好了）
#   1 = 达到最大轮数或 3-strike，未收敛
#   2 = 时间超限
#   3 = 环境错误（verify.sh 本身坏了）
#
# 用法：
#   bash loop/worker-loop.sh
#   MAX_ITER=3 MAX_SECONDS=120 bash loop/worker-loop.sh
set -uo pipefail

## 配置项（可通过环境变量覆盖）：
- PROJECT_ROOT 自动检测（cd "$(dirname "$0")/.." && pwd）
- MAX_ITER=${MAX_ITER:-5}
- MAX_SECONDS=${MAX_SECONDS:-300}
- CONSEC_FAIL_LIMIT=${CONSEC_FAIL_LIMIT:-3}
- TASK=${TASK:-"修复识别功能的 bug，使 ci/verify.sh 通过"}
- STATE_FILE="loop/STATE.md"
- VERIFY_CMD="bash ci/verify.sh"
- START_TIME=$(date +%s)

## 初始化：
- 输出启动信息（MAX_ITER/MAX_SECONDS/CONSEC_FAIL_LIMIT）
- 清空 STATE.md（: > "$STATE_FILE"）
- 环境检查：ci/verify.sh 存在且可执行，否则 exit 3

## 辅助函数：
- elapsed_seconds()：计算已运行秒数
- extract_fail_stage()：从 verify 输出中提取失败阶段（grep "第 N 层"）

## 主循环（for i in 1..MAX_ITER）：

每轮逻辑：
  1. 时间检查：elapsed >= MAX_SECONDS → exit 2
  2. 构建 CONTEXT：
     - 基本任务描述
     - 如果 STATE.md 非空，包含历史教训（"请根据上面的失败记录避开已尝试的方向"）
     - 如果 STRIKE_COUNT >= 2，追加 2-Strike 升级 prompt：
       "前两轮在同一个地方失败了。不要继续用类似方法修。
        请先：1. 分析 root cause  2. 提出不同思路  3. 再动手修"
  3. 调用 claude -p "$CONTEXT" --dangerously-skip-permissions
     - 捕获输出的最后 2000 字符
     - 提取前 80 字作为 AGENT_SUMMARY
  4. 运行 VERIFY_CMD，捕获输出和退出码
  5. 如果通过（exit 0）：
     - 记录成功到 STATE.md
     - 输出 "LOOP_EXIT code=0 reason=verify_pass elapsed=Xs rounds=N"
     - exit 0
  6. 如果失败：
     a. 从输出中提取 FAIL_LINE（grep VERIFY_FAIL）
     b. 提取 CURRENT_STAGE（失败在哪层）
     c. 追加本轮记录到 STATE.md：
        ## 第 N 轮 (HH:MM:SS)
        **尝试方向：** (前80字)
        **结果：** 失败
        **错误摘要：** (VERIFY_FAIL 行)
        ---
     d. Strike 计算：
        - 如果 CURRENT_STAGE == LAST_STAGE（连续同层失败）→ STRIKE_COUNT++
        - 否则 STRIKE_COUNT = 1
     e. 3-Strike 熔断：STRIKE_COUNT >= CONSEC_FAIL_LIMIT
        → 输出"3-Strike 熔断：连续 N 轮在 xxx 失败，需要人工介入"
        → "LOOP_EXIT code=1 reason=3-strike elapsed=Xs rounds=N"
        → exit 1

## 达到 MAX_ITER 后：
- 输出"达到最大轮数，未收敛"
- "LOOP_EXIT code=1 reason=max_iter elapsed=Xs rounds=N"
- exit 1

确保脚本可执行（chmod +x）。
```

---

## 验证提示词（Lab 13）

```
帮我验证 Lab 13 的 Worker Loop 是否完整：

1. 脚本存在且可执行：
   ls -la loop/worker-loop.sh

2. 语法检查：
   bash -n loop/worker-loop.sh && echo "语法OK" || echo "语法错误"

3. 快速验证（当前代码应该是绿的，loop 第 1 轮就通过）：
   cd /workshop/ypjh && MAX_ITER=2 MAX_SECONDS=30 bash loop/worker-loop.sh
   echo "exit code: $?"
   预期：exit 0（verify.sh 本身是绿的，第 1 轮直接通过）

4. 验证熔断（用一个必定失败的验证命令）：
   cd /workshop/ypjh && VERIFY_CMD="bash -c 'echo VERIFY_FAIL stage=test; exit 1'" MAX_ITER=2 MAX_SECONDS=10 bash loop/worker-loop.sh
   echo "exit code: $?"
   预期：exit 1（被轮数或 strike 熔断停止）

5. 检查 STATE.md 格式：
   cat loop/STATE.md
   应该有"第 N 轮"、"尝试方向"、"错误摘要"格式的记录

6. 检查退出摘要：
   最后一行应包含 "LOOP_EXIT code=..."

7. 检查关键功能点：
   - grep "STRIKE" loop/worker-loop.sh（应有 2-strike/3-strike 逻辑）
   - grep "MAX_SECONDS" loop/worker-loop.sh（应有时间熔断）
   - grep "STATE_FILE" loop/worker-loop.sh（应有 STATE.md 读写）

报告：正常通过 + 熔断生效 / 哪里有问题
```

---

# 附：每个 Lab 完成后的预期目录结构

```
/workshop/ypjh/
├── .claude/
│   ├── settings.json               # Lab 10+11: 全部 hooks 配置
│   ├── agents/
│   │   └── test-runner.md          # Lab 10: 测试子代理
│   └── hooks/
│       ├── capture-session.sh      # Lab 11: Stop hook 记 git 痕迹
│       ├── capture-correction.sh   # Lab 11: UserPromptSubmit 捕获纠正
│       └── inject-memory.sh        # Lab 11: SessionStart 注入+提醒
├── hooks/
│   ├── post-edit-check.sh          # Lab 10: 编辑后架构检查
│   └── session-start.sh            # Lab 10: 基础 SessionStart
├── memory/
│   ├── exit-contract.template.md   # Lab 10: 交接模板
│   ├── daily/                      # Lab 11: 日志目录
│   ├── corrections.md              # Lab 11: 纠正记录
│   ├── MEMORY.md                   # Lab 11: 蒸馏后持久记忆
│   └── archive.md                  # Lab 11: 衰减归档
├── skills/
│   ├── distill-memory/SKILL.md     # Lab 11: 蒸馏 skill
│   ├── evolve-memory/SKILL.md      # Lab 11: 进化 skill
│   └── memory-health/SKILL.md      # Lab 11: 健康报告
├── ci/
│   └── verify.sh                   # Lab 12: 三层验证
├── loop/
│   ├── worker-loop.sh              # Lab 13: Worker Loop
│   └── STATE.md                    # Lab 13: 排错轨迹（运行时生成）
├── rules/
│   ├── personal.md                 # Lab 10: 含人类 Gate 规则
│   └── proposed.md                 # Lab 11: 规则提案（evolve 产出）
└── backend/
    ├── services/recognition_service.py
    └── tests/test_recognition_service.py
```

---

# 附：常见问题排查

| 问题 | 原因 | 解决 |
|------|------|------|
| capture-correction.sh 报 jq not found | 环境没有 jq | 改用 grep/python 做 JSON 提取 |
| verify.sh 报 pydantic import error | 没装依赖 | pip install pydantic |
| worker-loop.sh 第 1 轮超时 | claude -p 慢或无网络 | 检查网络，或把 MAX_SECONDS 调大 |
| inject-memory.sh 无输出 | MEMORY.md 等文件都不存在 | 先运行一次 capture 或手动创建 |
| 2-Strike 不触发 | extract_fail_stage 提取为空 | 确保 verify.sh 失败时输出含"第 N 层" |
